language: node_js
services:
- docker

jobs:
  include:
  - stage: Test Application
    script: 
      - npm run lint
      - npm run build
      - npm test
  - stage: Deploy Docker Image
    script: docker-compose build
    deploy:
      script: echo "hello"